<!DOCTYPE html>
<html>

<head>
<title>ユーザ一覧</title>
</head>

<body>
    <h1>ユーザ一覧</h1>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>名前</th>
        </tr>
        <% @users.each do |user| %>
        <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
        </tr>
        <% end %>
    </table>

    <a href="/users/new">ユーザを追加する</a>
</body>

</html>